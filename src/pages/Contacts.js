import { StyledContacts } from "../Theme";
const Contacts = () => {
    return(
        <StyledContacts>
            <section className="contacts">
                awdadawd
            </section>
        </StyledContacts>
    )
}

export default Contacts;